import React from 'react'
import { Switch } from '@mui/material'

const ModeSwitchBtn = ({ toggleTheme, isDarkMode }) => {
    return (
        <div>
            <Switch />
        </div>
    )
}

export default ModeSwitchBtn
